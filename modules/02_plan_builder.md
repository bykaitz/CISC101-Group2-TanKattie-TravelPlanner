Module 2 — Plan Builder (Options → Days)” (time-of-day loop)

Create a short list of canditate activties (e.g., attractions, restuarants, parks). Each activity includes type, estimated duration, cost range, and distance.

Use a simple loop to build days:

for each day: pick Morning activity (near lodging) pick Midday activity (close by) pick Afternoon activity (different theme) pick Evening restaurant or optional event
